<div class="container">
  <div class="row">
    <%= render 'layouts/notifications' %>
  </div>
  <div class="row">
    <div class="mx-auto col-11 col-sm-10 col-lg-8 col-xl-6 px-3">
      <div class="introduction px-5 ">
        <p class="px-3 mx-auto">
          ようこそながのCAKEへ！</br>このサイトは、ケーキ販売のECサイトになります。
        </br>会員でない方も商品の閲覧はできますが、</br>購入には会員登録が必要になります。
        </p>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-3 mt-2 ml-5">
      <h3>新着商品</h3>
    </div>
  </div>
  <div class="row">
    <% @item.each do |item| %>
    <div class="col-2 mt-1 ml-2">
      <%= link_to public_item_path(item.id) do %>
        <%#= attachment_image_tag(item, :image, size: "60x60", fallback: "no-image.jpg") %>
        <%= item.name %>
        <%= (item.price * @tax).to_i.to_s(:delimited, delimiter: ',') %>
      <% end %>
    </div>
    <% end %>
    <div class="col-ofset-8">
      <%= link_to "全ての商品を見る＞", public_items_path %>
    </div>
  </div>
</div>
