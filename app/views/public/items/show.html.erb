<%# TODO:20210120注文できる個数の最小・最大値をチームで設定する（とりあえず現在1と1000） %>
<%# TODO:202101222ジャンル検索リンクがワイヤーフレーム（エンドユーザ側P6）上に見当たらないが、エンドユーザ側UI Flowsに記されているので勝手に作成したので、チームで共有する（ジャンル名を表示し、それがリンクになっている） %>

<%# <div class="container"> %>
<%# <div class="row"> %>
<%# <div class="column"> %>
<%= image_tag(@item.image_id, alt: "Item Image", id: "item-image") %>
<%# </div> %>
<%# <div class="column"> %>
<%= form_with model:@cart_item, local:true do |f| %>
<h3 id="item-name">@item.name</h3>
<p id="item-detail">@item.detail</p>
<div class="form-group">
  <p id="item-price"><span>￥<%= price_with_tax(item.price).to_s(:delimited, delimiter: ',') %></span>（税込み）</p>
  <%= link_to @item.genre_name, items_path("genre_id") %>
</div>
<%= f.number_field :count, id: "item-count", min:1, max: 1000,placeholder: "個数選択"%>
<%= f.submit "カートに入れる"%>
<% end %>
<%# </div> %>
<%# </div> %>
<%# </div> %>